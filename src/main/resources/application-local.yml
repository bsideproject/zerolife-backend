spring:
  application:
      name: zero
  datasource:
    url: jdbc:postgresql://118.67.134.86:5432/postgres?currentSchema=zerolife
    username: admin
    password: bside10-6

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
      use-new-id-generator-mappings: false
    generate-ddl: true
    open-in-view: false
    properties:
      hibernate:
        default_batch_fetch_size: 100
        dialect: org.hibernate.dialect.PostgreSQLDialect